import React from 'react';
import AppLayout from '@/layouts/app-layout';

const Calendar = () => {
    return (
        <AppLayout>
            hello calendar
        </AppLayout>
    );
};

export default Calendar;
