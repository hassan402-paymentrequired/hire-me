import React from 'react';
import AppLayout from '@/layouts/app-layout';

const Appointments = () => {
    return (
        <AppLayout>
            hello appointments
        </AppLayout>
    );
};

export default Appointments;
